{
  "name": "@fireball/docker-orchestrator",
  "version": "0.1.0-dev.5",
  "private": true,
  "description": "Multi-tenant Fireball remote-browser orchestrator foundation",
  "type": "module",
  "engines": {
    "node": ">=24 <27"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "npm run contracts:check && npm run deployment:check && npm run candidate:evidence:check && npm run seccomp:check && npm run session:image:check && npm run session:check && npm run build && npm test",
    "candidate:evidence:check": "node --check scripts/session-candidate-evidence.mjs && node --test test/session-candidate-evidence.test.mjs",
    "contracts:generate": "node scripts/sync-xanhtab-contract.mjs",
    "contracts:check": "node scripts/check-xanhtab-contract.mjs",
    "dev": "node --enable-source-maps dist/src/main.js",
    "deployment:check": "node --check scripts/render-nginx-config.mjs && node --check scripts/install-nginx-config.mjs && node --check scripts/real-nginx-tls-gate.mjs && node --test test/deployment-adapters.test.mjs",
    "session:check": "npm run check --prefix session",
    "session:image:check": "node --check scripts/real-isolation-gate.mjs && node --check scripts/real-browser-state-gate.mjs && node --check scripts/real-restart-crash-gate.mjs && node --check scripts/real-multi-tab-gate.mjs && node --check scripts/real-media-gate.mjs && node --check scripts/real-turn-gate.mjs && node --check scripts/fixtures/browser-state-server.mjs && node --check scripts/install-firefox-openh264.mjs && node scripts/install-firefox-openh264.mjs check && node scripts/check-session-image.mjs",
    "session:browser-state:smoke": "node scripts/real-browser-state-gate.mjs",
    "session:isolation:smoke": "node scripts/real-isolation-gate.mjs",
    "session:restart-crash:smoke": "node scripts/real-restart-crash-gate.mjs",
    "session:media:smoke": "node scripts/real-media-gate.mjs",
    "session:multi-tab:smoke": "node scripts/real-multi-tab-gate.mjs",
    "session:nginx-tls:smoke": "node scripts/real-nginx-tls-gate.mjs",
    "session:turn:smoke": "node scripts/real-turn-gate.mjs",
    "seccomp:check": "node --check scripts/seccomp-profile.mjs && node scripts/seccomp-profile.mjs check",
    "seccomp:generate": "node scripts/seccomp-profile.mjs generate",
    "test": "node --test dist/test/*.test.js"
  },
  "dependencies": {
    "@fastify/websocket": "11.3.0",
    "fastify": "5.12.0",
    "jose": "6.2.9",
    "ws": "8.21.3"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "@types/ws": "8.18.1",
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1",
    "openapi-typescript": "7.13.0",
    "typescript": "5.9.3"
  }
}
